% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{rank_env_windows}
\alias{rank_env_windows}
\title{Rank the strongest environmental windows}
\usage{
rank_env_windows(
  x,
  n = 10L,
  significant_only = FALSE,
  alpha = 0.05,
  min_pairs = NULL
)
}
\arguments{
\item{x}{An `env_window_scan` object or its `correlations` data frame.}

\item{n}{Number of windows retained per weather-variable/trait combination.}

\item{significant_only}{Keep only windows with adjusted p-values at or below
`alpha`.}

\item{alpha}{Adjusted-p significance threshold.}

\item{min_pairs}{Optional additional minimum number of environments.}
}
\value{
A ranked tibble. `rank = 1` is the strongest retained association
  within a weather-variable/trait combination.
}
\description{
Orders successful weather-window associations by the absolute value of their
correlation, separately for every weather-variable and trait combination.
}
\examples{
dat <- simulate_env_window_data(n_environments = 16, n_days = 100,
                                signal_start = 30, signal_end = 55)
fit <- scan_env_windows(
  dat$weather, dat$traits, dat$periods,
  weather_vars = "temperature", trait_vars = "yield",
  window_sizes = c(14, 28), step = 7, min_pairs = 8, quiet = TRUE
)
rank_env_windows(fit, n = 5)
rank_env_windows(fit, n = 5, significant_only = TRUE, alpha = 0.10)
}
