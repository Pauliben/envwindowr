% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_window_heatmap}
\alias{plot_window_heatmap}
\title{Plot a correlation heatmap across window starts and ends}
\usage{
plot_window_heatmap(
  x,
  weather_var,
  trait,
  show_significance = TRUE,
  alpha = 0.05
)
}
\arguments{
\item{x}{An `env_window_scan` object or correlation-results data frame.}

\item{weather_var}{Weather variable name exactly as supplied to
[scan_env_windows()].}

\item{trait}{Trait name exactly as supplied to [scan_env_windows()].}

\item{show_significance}{Mark windows whose adjusted p-value is at or below
`alpha` with an X.}

\item{alpha}{Adjusted-p threshold used when `show_significance = TRUE`.}
}
\value{
A `ggplot` object that can be further customized with ggplot2.
}
\description{
Displays correlation coefficients for all candidate start and end offsets for
one weather-variable/trait combination.
}
\examples{
dat <- simulate_env_window_data(n_environments = 16, n_days = 100,
                                signal_start = 30, signal_end = 55)
fit <- scan_env_windows(
  dat$weather, dat$traits, dat$periods,
  weather_vars = "temperature", trait_vars = "yield",
  window_sizes = c(14, 28), step = 7, min_pairs = 8, quiet = TRUE
)
plot_window_heatmap(fit, "temperature", "yield")
}
